schema_version = 1
id = "demo-fixture"
display_name = "Demo Fixture"
target_runtime = "Codex CLI"
detect_binaries = ["demo-fixture"]
launch_args = []

[package]
kind = "manual"
instructions = "manual"

[isolation]
home_subdirs = [".codex"]
static_envs = { CODEX_HOME = "{home}/.codex" }
seed_files = []
